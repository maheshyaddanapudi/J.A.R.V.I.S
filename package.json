{
  "name": "jarvis",
  "private": true,
  "version": "0.1.0",
  "description": "J.A.R.V.I.S. — local-first personal AI OS (see CLAUDE.md and docs/)",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
