# Night Lab (D-0079) — implementation + live campaign verification, 2026-08-28

**Scope:** full implementation of `docs/NIGHT_LAB_SPEC.md` (slices L1a, L1b, L2, L3, L4)
and a real end-to-end campaign night run live against the Anthropic API (Sonnet 5),
fired through the REAL scheduler path — not a shortcut route. Requirements R-LAB-01…10;
threats T-LAB-1…6 (THREAT_MODEL §T15).

Everything below is a real observation from this container. No mocked results.

## 1. What was built (commit per slice)

| Slice | Commit | Content |
|---|---|---|
| L1a | `395787d` | Judge prompts → PromptRegistry kind `template` (5 seeded templates, absent-only, registry override honored, deterministic fallback) |
| L1b | `cbc1a49` | The bench: `bench/` (fixtures, 10 rubric conversations, 4-dim grading rubric, campaign contract) + `scripts/lab_bench.py` (isolated lab kernel on `jarvis_lab`/port 4571, 8 deterministic hard gates, rubric grading via `/gateway/chat`, sha256 bench hash) |
| L2 | `65dbfd4` | Experiment engine: `lab/surface.ts` (Z1 allowlist + forbidden-prefix deny-first, R-CAP-08 protected), `lab/engine.ts` (N=3 trials, δ=4 mean margin, ε=3 guard bands, hard-gate auto-discard, crash rows), migration 0026 `lab_experiments`, `lab/researcher.ts` (planning-role candidate generation, ONE change per candidate) |
| L3 | `55aa6c5` | Scheduling: `lab/night.ts` (quiet-hours slot at END of scheduler tick, skip/halt conditions, morning report from ledger only), settings `lab.enabled`/`lab.campaign`/`budget.lab.nightlyTokenCap`, Budget source `night-lab` |
| L4 | `0dfba4d` | Apply-to-live: `lab/apply.ts` (three envelopes, D-0052 pin refusal, exact revert from captured prior state), routes `/lab/*`, Command Center `/lab` panel |

Test suite: **431 kernel tests, 431 pass** (50 files, final run 2026-08-28 00:21 UTC, concurrent with the live night — separate `jarvis_test` DB). 39 new: 17 engine/surface + 12 night + 7 apply + 3 judge-template.

## 2. Live campaign night — real API, scheduler-fired

**Setup (all through the real HTTP API, un-seeded discipline):**
- Fresh live kernel on scratch DB `jarvis_d79`, port 4150, 26 migrations, workspace `/tmp/d79-ws`.
- Role pins (env): `fast_conversation` → `anthropic/claude-sonnet-5@medium+thinking`, `planning` → `@high+thinking`, `deep_reasoning` → `@xhigh+thinking`. Real `ANTHROPIC_API_KEY` in kernel env only.
- Settings set via `PUT /settings/...` as user: `lab.enabled=true` (the D-0079 check-in enable), `proactive.quietHours.end=3`, `budget.lab.nightlyTokenCap=150000`, `autonomy.enabled=true`, `autonomy.intervalMinutes=120`. Quiet-hours window 22→03; night fired at ~00:20 UTC (inside window).
- Judge templates confirmed boot-seeded: 5 `template` prompts present via `GET /prompts`.

**Firing path:** `POST /autonomy/tick` — the real BackgroundScheduler tick (proactive →
sleep-cycle → heartbeat → **labNight at END of tick**). This proves the scheduler→lab
integration, not just the `/lab/night` convenience route.

**Observed run (from the ledger + processes, as it happened):**
- The tick spawned `scripts/lab_bench.py --db jarvis_lab --port 4571` (baseline, no candidate file); the bench spawned its own isolated kernel — live kernel untouched.
- **Baseline (persona-adherence campaign):** persona **93.2**, comprehension 93.3, memory 93.0, honesty 93.7 — all 8 hard gates PASS. Cached for the night.
- **Experiment 1:** researcher (planning role) proposed a full butler-persona rewrite encoding all four campaign hypotheses (address convention, one-remark wit budget, dissent-then-comply, anti-collapse, brevity). Envelope `proposal` (persona kind — never auto-applied). Trial 1: persona **92.0** ≤ baseline 93.2 → **DISCARD** after one trial (must-beat-baseline short-circuit; trials 2–3 not spent). Guard dims on that trial: comprehension 95.0, memory 95.2, honesty 92.0 — gates all pass; the candidate simply wasn't better. Tokens: 33,176. Bench hash `89106a839a65e69f` stamped on the row.
<!-- REMAINING EXPERIMENTS + MORNING REPORT + APPLY/REVERT + SCRUB PENDING -->
