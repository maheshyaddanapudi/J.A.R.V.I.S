# Longitude-XL gap ledger (living document)

**Purpose.** One place for every gap the 1000-day run has exposed — in the memory system, in
the agent's behaviour, in the experiment design, and in the instrument — with the evidence
that backs it and the candidate refinement. This is the worklist for the refinement pass
after the run; nothing here is fixed mid-run (instrument-vs-system doctrine, D-0080 records).
Last updated at simulated day 898 (2026-09-10). The final numbers land in the second-act record.

## Where the evidence lives

| Store | What it holds | Location |
|---|---|---|
| Quiz answers | every battery: per-fact records (`layer`, `hit`, `honest_miss`, `age`, `flips`, `truth`, answer segment) **and the raw model answers** — re-scorable under any rubric | `/tmp/longitude_xl/quizzes.jsonl`, copied into every `docs/verification/longitude_xl/day<N>/` snapshot |
| Daily metrics | one row per simulated day: memory counts, history rows, vectors, topics, autotune, lab tick, latency, spend | `metrics.jsonl` (same places) |
| Run log | day lines, pins/re-pins, restarts, chapter-two start, rewinds, halts | `run.log` (+ `run.log.gz` in snapshots) |
| Kernel world | encrypted memory, preferences with history, relations, episodes, embeddings, `reasoning_decisions`, `lab_experiments`, hash-chained `audit_log`, `model_calls` | DB `jarvis_xl`; dumps at day 500 (root), 600, 700, 800 (+900, 1000) under `docs/verification/longitude_xl/` |
| Attempt 1 of the second act (flawed selector, days 501–581) | full DB dump + checkpoint + quiz/metric rows | `/tmp/longitude_xl/jarvis_xl.act2-attempt1-day581.sql.gz`, `docs/verification/longitude_xl/*.act2-attempt1.jsonl` |
| Replay instrument | 55 preserved first-act misses re-asked through the recall endpoints: baseline / S1 / S4 | `docs/verification/longitude_xl/replay_*.json` |
| Human-readable ledger | daily table, batteries by layer, milestone map | `scripts/longitude_xl_report.py`; page "The Longitude Ledger" |
| Decisions and specs | D-0080 (four defects, fixes, verification), open questions | `docs/RETRIEVAL_FIDELITY_SPEC.md`, `docs/DECISION_LOG.md`, `docs/verification/RETRIEVAL_FIDELITY_2026-09-01.md` |

## Gaps

Classes: **W** write-side (never stored / stored in the wrong home) · **R** read-side (stored, not retrieved or the wrong entity read) · **T** traversal (multi-hop) · **M** model behaviour (tool choice / arguments) · **D** experiment design · **I** instrument (harness / scorer) · **O** operations.

| ID | Class | Gap | Evidence (second act, days 501–898 unless noted) | Status | Candidate refinement |
|---|---|---|---|---|---|
| G-01 | W (first act, legacy) | Facts that were never stored in the first act keep returning as honest "not found" — the memory cannot retroactively hold what the old kernel dropped | Recurring honest misses per topic: weekend preferred meeting day 7/9, tea order 4/10, sensor importer two 3/6, lab-glass supplier 2/5, microscope 2/3, boat shed 2/3, test range two 2/2, glacier telemetry 2/6, plus one-offs (lab-glass supplier north, archive digitisation, lakeside cabin two, filament shop, irrigation controller, storage unit). The replay instrument classed 11 of 55 first-act misses as absent from every store | Open; cannot be fixed in the world, only prevented | A **teach receipt**: after a teaching turn the agent re-reads what it stored and reports per statement (the batch `memory.rememberFacts` already does this for multi-statement teaches; extend the read-back to single writes and to preference writes) |
| G-02 | R | Twin/neighbour confusion on single-hop recall: asked about `coral census`, the agent answers from `coral census two`; `test range` → `test range north` | `coral census` 4/4 wrong (always the twin), `test range` 1/2; the scorer marks these as wrong values, not honest misses | Open | When exact-name recall finds nothing, the tool should list near names as **different entities** and the agent should answer "not found for X (I do know X two)". Identity-first seeding (D-0080 S1) fixed the graph seed; the exact-recall tool's fallback wording is the remaining lever |
| G-03 | W (first act, legacy) + R | Two homes for one attribute: a value corrected before D-0080 left duplicates (or an old preference) and the agent now sees "conflicting records" and picks the stale one | `microscope two` 3/4 (answers ochre, truth cobalt, explicitly "conflicting records found"), `coral census north` 3/7 (palladium vs graphene), `sensor importer two` 3/6 (cusco vs lisbon), `morning swim` 1/8, `tidal model north` 1/7, `filament shop` ("two conflicting values") | Open | A **reconciliation pass** in the quiet-hours consolidation: two active facts on the same entity+attribute slot (same slot words, different values) → supersede the older with history, announce; same for an entity fact vs a preference holding the same attribute. This is the read-side twin of D-0080 B1 |
| G-04 | T | Multi-hop traversal picks a twin on the middle hop: `roof array` resolved to `rooftop garden two` / `roof array north`, `kiln` → `kiln north`, `aquarium rig` → `aquarium rig two` | 8 of 72 two-hop misses; 6 of the 8 are twin substitutions on the device hop | Open | Apply the S1 identity rule inside `traverse`/`memory.related` name resolution (exact name before similar names; never silently substitute a longer twin), and make the agent's answer name the entity it actually traversed |
| G-05 | M / W | A first-person preference stored as a fact on a synthetic entity named "user" (`my gym day is friday` → entity `user`), invisible to `recallPreferences` and to graph seeding for "my gym day" | 1 fact (day 585); `gym day` missed 3/6 as honest "not found" | Open (isolated) | `memory.recallPreferences` also surfaces facts of `user`/`me` entities; or the write path routes "my X is Y" statements to the preference store |
| G-06 | W / R (to verify) | Chapter-two preference `study plant` missed 2/4 as "not found" although delivered | 2/4 | Investigate at the end of the run (which key it landed under; whether the dup-tidy or key naming hid it) | — |
| G-07 | T / D | Relationship conflicts between chapters are not reconciled: the base layer taught "diego mbeki maintains weather mast north", chapter two taught "quinn ferreira maintains weather mast north"; the agent answered "not found — maintained by diego mbeki, not quinn ferreira" | 1 two-hop miss (day 730) | Instrument/design | Relations have no supersession: same (verb, object) with a new subject should supersede with history, or the harness should not create the collision. Decide which at refinement |
| G-08 | D | Night Lab produced 1 keep in ~60 experiments; most nights halt on the nightly token cap before three trials complete | `lab_experiments`: keep 1, discard 58, crash 1; cap 60,000 tokens vs 70–95k observed per night | Open | Raise `budget.lab.nightlyTokenCap` for the bench size, or shrink the bench so N=3 fits; measure keep rate per completed night rather than per started night |
| G-09 | D | No new deep-reasoning learning was exercised late in life: chapter two introduced no deep-topic corrections, so D-0050 promotion was only tested in days 6–68 | `topics` constant at 7 from day 68 (after `tuning` removal at 501) | Design gap | A chapter-three layer with late-life deep-topic corrections (and a junk-word control) to test learning by correction after 500+ days |
| G-10 | D | The D-0052 pin/override arc was dormant in the second act: threshold 1 by J.A.R.V.I.S. since day 35, no user pins after day 24 | `autotune` unchanged 35→898 | Design gap | Schedule a late re-pin in the next act so the re-pin-scaled bar (×3) is actually exercised |
| G-11 | M | Agent habits the kernel now absorbs: `rememberFact` for an update (write guard refuses, agent re-issues `memory.correct`; ~1 per simulated day), the preference key passed as `entity`/`replaces`, dropping "two" from a twin's name when correcting | Audit: every refused `rememberFact` since day 501 was followed by a successful `memory.correct` within seconds | Mitigated | Keep the guards; measure the ratio as a health metric; consider a tool-description nudge for twin names |
| G-12 | I | Nickname collision: chapter two contains two people called Pavel and the harness assigned "pavel" as a nickname; the agent's "two matches" answer is correct and scored as a miss | 4 nickname questions affected; excluded from the nickname score | Instrument flaw | Unique-first-name check in `build_expansion` next act (changing it now would trip the chapter-two drift guard) |
| G-13 | I | Scorer leniency: the truth string anywhere in the answer segment counts ("lakeside cabin north" satisfies "lakeside cabin"); two-hop questions have one chain shape only; retirement answers scored by a closed-word regex | Known since the first act; both arms of every comparison scored identically | Accepted, disclosed | Strict re-score pass under `quizzes.jsonl` (raw answers kept for exactly this) |
| G-14 | O | Provider credit exhaustion (attempt-1 day 540, attempt-2 day 831) and container idle-freezes (four so far) | The same-day halt (`assert_day_live`) stopped day 831 before it was committed; freezes cost wall-clock only | Mitigated | Keep the guard; the hourly server-side Routine now relaunches after a freeze |
| G-15 | O | Judge templates are re-seeded into the prompts registry on every boot as new version-1 rows | `prompts` table grows one row per template per restart | Cosmetic | Seed only when absent, or bump the version and dedupe by content |

## How to refine from here

1. After day 1000: strict re-score of every battery from the raw answers (G-13), then re-derive the layer numbers.
2. Rank G-01…G-07 by attributable misses (G-01 and G-03 dominate the old-world 9%; G-02 and G-04 are the twin family).
3. Each fix gets the D-0080 treatment: spec, unit tests from the exact captured tool arguments, a Sonnet-5 mini-life, then a third act on the preserved day-1000 snapshot with chapter three (G-09, G-10) so the field verification stays on one kernel build.
