# Longitude-XL gap ledger (living document)

**Purpose.** One place for every gap the 1000-day run has exposed — in the memory system, in
the agent's behaviour, in the experiment design, and in the instrument — with the evidence
that backs it and the candidate refinement. This is the worklist for the refinement pass
after the run; nothing here is fixed mid-run (instrument-vs-system doctrine, D-0080 records).
Final numbers as of simulated day 1000 (2026-09-11); the second-act record is
`LONGITUDE_XL_ACT2_2026-09-11.md`. Every "(second act)" figure below covers days 501–1000
unless a day range is given.

## Where the evidence lives

| Store | What it holds | Location |
|---|---|---|
| Quiz answers | every battery: per-fact records (`layer`, `hit`, `honest_miss`, `age`, `flips`, `truth`, answer segment) **and the raw model answers** — re-scorable under any rubric | `/tmp/longitude_xl/quizzes.jsonl`, copied into every `docs/verification/longitude_xl/day<N>/` snapshot |
| Daily metrics | one row per simulated day: memory counts, history rows, vectors, topics, autotune, lab tick, latency, spend | `metrics.jsonl` (same places) |
| Run log | day lines, pins/re-pins, restarts, chapter-two start, rewinds, halts | `run.log` (+ `run.log.gz` in snapshots) |
| Kernel world | encrypted memory, preferences with history, relations, episodes, embeddings, `reasoning_decisions`, `lab_experiments`, hash-chained `audit_log`, `model_calls` | DB `jarvis_xl` (preserved — the third act runs on it); dumps at day 500 (root), 600, 700, 800, 900, 1000 under `docs/verification/longitude_xl/` |
| Attempt 1 of the second act (flawed selector, days 501–581) | full DB dump + checkpoint + quiz/metric rows | `/tmp/longitude_xl/jarvis_xl.act2-attempt1-day581.sql.gz`, `docs/verification/longitude_xl/*.act2-attempt1.jsonl` |
| Replay instrument | 55 preserved first-act misses re-asked through the recall endpoints: baseline / S1 / S4 | `docs/verification/longitude_xl/replay_*.json` |
| Human-readable ledger | daily table, batteries by layer, milestone map | `scripts/longitude_xl_report.py`; page "The Longitude Ledger" |
| Decisions and specs | D-0080 (four defects, fixes, verification), open questions | `docs/RETRIEVAL_FIDELITY_SPEC.md`, `docs/DECISION_LOG.md`, `docs/verification/RETRIEVAL_FIDELITY_2026-09-01.md` |

## Gaps

Classes: **W** write-side (never stored / stored in the wrong home) · **R** read-side (stored, not retrieved or the wrong entity read) · **T** traversal (multi-hop) · **M** model behaviour (tool choice / arguments) · **D** experiment design · **I** instrument (harness / scorer) · **O** operations.

| ID | Class | Gap | Evidence (second act, days 501–1000 unless noted) | Status | Candidate refinement |
|---|---|---|---|---|---|
| G-01 | W (first act, legacy) | Facts that were never stored in the first act keep returning as honest "not found" — the memory cannot retroactively hold what the old kernel dropped | Recurring honest misses per topic: weekend preferred meeting day 7/9, tea order 4/10, sensor importer two 3/6, lab-glass supplier 2/5, microscope 2/3, boat shed 2/3, test range two 2/2, glacier telemetry 2/6, plus one-offs (lab-glass supplier north, archive digitisation, lakeside cabin two, filament shop, irrigation controller, storage unit). The replay instrument classed 11 of 55 first-act misses as absent from every store | **PREVENTED 2026-09-11** (`REFINEMENT_2026-09-11.md` R6) — `memory.rememberFact` and `memory.remember` confirm every write by reading it back and quote the stored text; a write that does not read back is `ok:false`. The old kernel's dropped facts cannot be recovered in the world: chapter three re-teaches them (the harness re-states the facts a user would notice missing) and the strict scorer checks they land. **R10 replay (2026-09-11)** of the second act's 35 unique misses on a scratch restore of the day-1000 world: 5 facts absent from every store (`coral census` service day, `test range` home city, `lakeside cabin two` home city, `roof array two` status colour, `irrigation controller two` assigned number) — all five in the chapter-three re-teach set (`replay_after_r10.json`) | Closed as PREVENTED; world-side closure in act three (T2) |
| G-02 | R | Twin/neighbour confusion on single-hop recall: asked about `coral census`, the agent answers from `coral census two`; `test range` → `test range north` | `coral census` 5/5 wrong (always the twin), `test range` 1/2, `roof array two`, `cloud recycler`, `kiln north` one each; the scorer marks these as wrong values, not honest misses. 21 wrong-value answers on single-hop facts in the act, every one a stored value. Root cause: G-17 (the short name was an ALIAS of the twin) | **FIXED+VERIFIED 2026-09-11** (`REFINEMENT_2026-09-11.md` R3) — a twin alias never resolves the short name (`findEntity`, identity seeding); a miss names the siblings as DIFFERENT entities; agent rule. Day-1000 world: `coral census` now answers "not found — Coral Census Two / north exist but are different entities" | When exact-name recall finds nothing, the tool should list near names as **different entities** and the agent should answer "not found for X (I do know X two)". Identity-first seeding (D-0080 S1) fixed the graph seed; the exact-recall tool's fallback wording is the remaining lever |
| G-03 | W (first act, legacy) + R | Two homes for one attribute: a value corrected before D-0080 left duplicates (or an old preference) and the agent now sees "conflicting records" and picks the stale one | `microscope two` 3/4 (answers ochre, truth cobalt, explicitly "conflicting records found"), `coral census north` 3/7 (palladium vs graphene), `sensor importer two` 3/6 (cusco vs lisbon), `morning swim` 1/8, `tidal model north` 1/7, `filament shop` ("two conflicting values"). Strict re-score: 45 hedges + 52 misattributed answers across both acts. Three shapes in the world: fact vs fact, fact vs preference, fact vs the entity's free-text ATTRIBUTES (written at first mention, never corrected) | **FIXED+VERIFIED 2026-09-11** (`REFINEMENT_2026-09-11.md` R4) — `reconcileHomes` in the quiet-hours sleep cycle + `POST /memory/reconcile-homes`: the newer record wins, the older is retired with history (fact superseded / preference soft-deleted / attribute clause removed, all audited), announced. Applied to the day-1000 world: 12 conflicts (7 stale preferences, 1 stale fact, 4 stale attribute clauses); the hedged questions now answer cleanly (microscope two ochre, lab-glass supplier two 3, umar brandt Bergen, …). Sonnet 5 on the scratch world: "conflicting records" → clean answers. 5 tests; suite 512/512 |
| G-04 | T | Multi-hop traversal picks a twin on the middle hop: `roof array` resolved to `rooftop garden two` / `roof array north`, `kiln` → `kiln north`, `aquarium rig` → `aquarium rig two` | 11 two-hop misses in 108 asked (89.8 % lenient; **63.9 % strict** — 22 twin substitutions credited by the lenient rule); 7 lenient misses are twin substitutions on the device hop (`roof array` ×5, `kiln`, `aquarium rig`), 2 honest refusals on conflicting `located_in` links (`microscope two`, days 960/990 — G-07), 1 cross-chapter maintainer conflict (G-07), 1 truncated answer (instrument). Root cause: G-17 aliases, plus the agent leading with a look-alike's chain | **FIXED on the kernel side, residue recorded** (R3) — graph-recall output tags look-alikes "a DIFFERENT entity from 'X'", prints "no connections recorded for X", closes with "answer only about X"; agent system rule. Sonnet 5: `field pump north — rosa castillo` → honest not found; bare-name `field pump — rosa castillo` still leads with "rosa maintains field pump two (the plain field pump is a different entity…)". **R10 (2026-09-11):** the residue reproduced in the mini-life (asked about `brine pump north`, which has no location, the agent answered "not found" but LED with the base pump's chain) → lever added on the kernel side: `memory.lookup` prints "↔ no connections recorded for X" explicitly, the lookup/graph closings and the agent rule say OPEN with 'not found' (a look-alike note may follow, never lead); measured in mini-life run 2 and under the strict scorer in act three (T4) | Apply the S1 identity rule inside `traverse`/`memory.related` name resolution (exact name before similar names; never silently substitute a longer twin), and make the agent's answer name the entity it actually traversed |
| G-05 | W / M | **First-person preferences written as entity facts.** "Remember: my X is Y" is stored by the agent with `memory.rememberFact` on an entity named after the thing (or after the speaker), never as a preference — so `memory.recallPreferences` cannot see it, and a battery that consults only the preference store says "not found" | Three chapter-two preferences, traced in the audit log: `gym day` → entity `user` (day 585), `bike colour` → entity `bike` (seq 17594–17598, day 616), `study plant` → entity `study plant` (seq 19131–19135, day 665). All three ARE in the store and were recalled whenever the agent consulted the graph — gym day 4/8, bike colour 5/6, study plant 1/3; the misses are the batteries where it consulted only `recallPreferences` (which returned `desk_plant` / `weekend_desk_plant` and 16 keys containing "colour"). **These three facts are all 7 of chapter two's 7 misses** (268/275) | **FIXED+VERIFIED 2026-09-11** (`REFINEMENT_2026-09-11.md` R2) | (1) `preferenceInDisguise` write guard in `memory.rememberFact`/`rememberFacts` hands back the exact `memory.remember` write; description nudge; (2) `recallPreferences(memory, entities)` also reports first-person facts on the subject's entity and on the user's own entity. 7 tests from the three captured objectives; suite 495/495; Sonnet-5 manual: all three teaches went to `memory.remember`, quiz 3/3; preserved day-1000 world (read-only): the misfiled facts now surface through `recallPreferences` — 3/3 + desk plant |
| G-06 | — | *(resolved into G-05 on 2026-09-11)* Chapter-two preference `study plant` "not found" although delivered | Audit trace: written as a fact on a new thing `study plant`, not under any preference key; the dup-tidy and key naming were not involved | Closed → G-05 | — |
| G-07 | T / D | Relationship conflicts are not reconciled: the base layer taught "diego mbeki maintains weather mast north", chapter two taught "quinn ferreira maintains weather mast north"; the agent answered "not found — maintained by diego mbeki, not quinn ferreira". The same shape on `located_in`: `microscope two` carries two location links (`boat shed`, `cold cellar two`) and the agent refuses rather than guess | 3 two-hop misses (days 710, 960, 990) — the refusals are the right behaviour on conflicting data, scored as misses. Day-1000 world: 9 devices with >1 maintainer of record, 5 things with >1 location | **FIXED+VERIFIED 2026-09-11** (`REFINEMENT_2026-09-11.md` R5) — migration 0027 `memory_relation_history`; exclusive relations (`located_in`… per subject, `maintains`/`owns`… per object) REPLACE the previous edge with history + `relation_superseded` audit, `additive:true` keeps both; `reconcileRelations` (route + sleep cycle) keeps the newest edge for a pre-rule world and SKIPS twin-touched anchors. Applied to the day-1000 world: 4 maintainer groups resolved (all chapter-two cross-links), 10 twin-touched groups skipped → re-teach. Sonnet 5: maintainer change and location move both landed with history. 3 tests; suite 515/515 | Relations have no supersession: same (verb, object) with a new subject should supersede with history, or the harness should not create the collision. Decide which at refinement |
| G-08 | D → **corrected** | Night Lab produced 1 keep in 67 experiments. The earlier reading ("most nights halt on the cap before three trials complete") was wrong: 65 discards were overwhelmingly "no improvement on first trial" (N=3 continues only after trial 1 improves — by design) + 5 honesty guard-band breaches; of 46 finished nights 11 halted on the cap. The real cap defect: checked only BETWEEN experiments, so a night on the XL world's 60k override ran to 108k–125k | `lab_experiments` at day 1000: keep 1, discard 65, crash 1; 2.54 M tokens over the life (the keep cost 73 k); XL override `budget.lab.nightlyTokenCap = 60000` (code default 300,000); `lab_night_finished` halts: 11 × cap, 3 × no candidates | **FIXED 2026-09-11** (R7) — an experiment starts only if the remaining budget covers a full one (3 × baseline or the campaign's observed mean); `completed` on the night summary; keep rate per completed night in the morning report. Test added | Raise `budget.lab.nightlyTokenCap` for the bench size, or shrink the bench so N=3 fits; measure keep rate per completed night rather than per started night |
| G-09 | D | No new deep-reasoning learning was exercised late in life: chapter two introduced no deep-topic corrections, so D-0050 promotion was only tested in days 6–68 | `topics` constant at 7 from day 68 to day 1000 (after `tuning` removal at 501); 9 auto-escalations in the act, all on learned topics | **DESIGNED-INTO-ACT-3 2026-09-11** (R9) — chapter three corrects four new deep topics twice each (days 1006–1044), asks two activity-word junk controls with the identical phrasing (1061–1075) and probes both junk words on auto (1101/1102); pre-registered target T7: 4/4 promoted within 10 days, 0/2 junk, probes stay fast | Closes with the act-three record **ACT-THREE VERDICT (2026-09-14): PARTIALLY MET — 3 of 4 chapter-three deep topics promoted** (`lidar calibration`, `tidal turbines`, `ion thrusters`); `cryogenic pumps` did NOT, and its two corrections (days 1006, 1008) fell inside the pre-swap rate-limit window where 30 % of fast-role calls failed, so the promotion machinery never received them. **Junk control perfect: 0 of 2** (`scheduling`, `tidying` never promoted). Verdict attributed to the provider window, not the promotion logic; the window is what forced D-0086. |
| G-10 | D | The D-0052 pin/override arc was dormant in the second act: threshold 1 by J.A.R.V.I.S. since day 35, no user pins after day 24 | `autotune` unchanged 35→1000 (965 days) | **DESIGNED-INTO-ACT-3 2026-09-11** (R9) — a deliberate late user pin on day 1010 (kernel re-pin #3 → bar 24), one more harness re-pin allowed after the next override (→ bar 30); the old day-35 override is acknowledged first so the pin is a decision, not a reflex; target T8: the contract holds at the raised bars | Closes with the act-three record **ACT-THREE VERDICT (2026-09-14): MET — the D-0052 contract held twice, end to end.** Day-1010 late user pin at threshold 2; override to 1 only once 24 contradictions since the pin cleared the bar of 24, **with an `announcements` row (`source='sleep-cycle'`) carrying the change, the tally, the bar and the re-pin term**; harness re-pinned (#4) raising the bar to 30; second override fired at 31 ≥ 30, also announced (2 announcement rows total). No J.A.R.V.I.S. write to the user setting below the bar. |
| G-11 | M | Agent habits the kernel now absorbs: `rememberFact` for an update (write guard refuses, agent re-issues `memory.correct`; ~1 per simulated day), the preference key passed as `entity`/`replaces`, dropping "two" from a twin's name when correcting | Audit, days 501–1000: 6 refused `rememberFact` writes, each followed by a successful `memory.correct` within seconds; 133 `memory.correct` calls, 0 refused. R5 also saw a location stored as a FACT rather than a `located_in` relation (invisible to traversal) | **FIXED 2026-09-11** (R7) — descriptions: connecting statements are relations (`memory.relate`), full entity names exactly as taught in `relate`/`correct`; `GET /ops/health` → `memoryHygiene` counts refused writes, corrections, merges, twin declines, splits, reconciliations and relation supersessions over 7 days (test) | Keep the guards; measure the ratio as a health metric; consider a tool-description nudge for twin names |
| G-12 | I | Nickname collisions: **three of the four** chapter-two handles collide with another person's first name — "pavel" (three Pavels), "theo" (Theo Eriksen / Theo Kowalski), "hiro" (Hiro Hoffmann / Hiro Ivanova); only "ravi" is unique. The agent's "two matches — which one?" answers are correct and were lenient hits whenever the truth day appeared among the candidates | Strict re-score (2026-09-11): pavel 0/14, the other three handles 21/26 — every strict miss is a collision answer | **FIXED 2026-09-11** (R9, instrument) — chapter three draws first names without replacement from a list disjoint from both earlier chapters and asserts at build time that every nickname occurs exactly once among ALL people of the world; third-act nickname questions use only world-unique handles (ravi + jiro, lucan, rashid, halvard), so the chapter-two collisions are never asked again; chapter two's hash untouched | Target T5: 100 % on unique handles |
| G-13 | I | Scorer leniency: the truth string anywhere in the answer segment counted; uncommitted "conflicting values" answers and twin-traversal answers were credited | **CLOSED 2026-09-11** — strict re-score of all 2,166 answers (`scripts/longitude_xl_rescore.py`, record `LONGITUDE_XL_RESCORE_2026-09-11.md`): first act 87.9 % → **82.4 %**, second act 93.0 % → **90.4 %** (+8.0 pp), old world 91.3 % → **87.7 %** (+5.3 pp — the pre-registered +5 is met under strict too), chapter two unchanged 97.5 %, two-hop 89.8 % → **63.9 %**. 117 downgrades: 45 hedges, 52 misattributed, 19 twins, 1 wrong; monotone (nothing added back) | Closed; **adopted live 2026-09-11** (R9) — the rubric is one module (`scripts/longitude_xl_strict.py`) used by the re-score instrument (byte-identical rows on re-run) and by the harness from the third act on: every record scored under both rubrics, full answers kept, raw rows untruncated | — |
| G-14 | O | Provider credit exhaustion (attempt-1 day 540, attempt-2 day 831) and container idle-freezes (several; one of six days) | The same-day halt (`assert_day_live`) stopped day 831 before it was committed and the run resumed there with no rewind; freezes cost wall-clock only (13.7 h of run time for the act) | **DOCUMENTED 2026-09-11** (R9) — `docs/verification/longitude_xl/RUNBOOK_ACT3.md`: launch checklist (incl. the kernel-health-with-0-pending step, added after the :4160 kernel was found running a stale build in memory), the act-three Routine prompt, keepalive cron, log Monitor, preserve marks, guards, never-list, pre-registered targets T1–T12. The Routine (`trig_01N8PRfNALCa6PhWBcXgt4bm`) re-armed at launch 2026-09-11 23:43 with a credit-probe-and-relaunch rule; target T12: no void day committed, freezes recovered within ~1 h. **First incident of act three (2026-09-11 23:43):** credits exhausted on day 1001 — `assert_day_live` halted before any commit (0 void days); resumes when credits are added | Re-armed; closes with the act-three record |
| G-15 | O → **system** | Judge templates were re-seeded on every boot as new version-1 rows — and the real cause was worse than cosmetic: `PromptRegistry.set()` deactivated EVERY prompt of a kind (right for the single persona, wrong for five named templates), so after each boot only the last-seeded template was active and served; the other four fell back to code constants, and registry edits to them never reached the model | 79 template rows in the day-1000 world, one active per name only by accident of seeding order | **FIXED 2026-09-11** (R3) — one active row per NAME for templates/system prompts (persona keeps one per kind); `seedJudgeTemplates` re-seeds only rows it wrote itself when the built-in text changes; tests |
| G-16 | R | Retirement status looked up on a twin: asked "is the kiln still active?" after `kiln` was retired on day 840, the agent searched for a status field on `kiln two` and `kiln north` and said "not found" | 2 of 40 retirement questions (days 890, 990), both `kiln`; the other five retirements 38/38. The entity table explains it: `kiln north` carries the aliases `{kiln, "the kiln"}` (G-17) | **FIXED 2026-09-11** (R3) — `kiln` split back out of `kiln north` (article variants `kiln`/`the kiln` resolve to one entity either way); Sonnet 5 on a fresh family: `field pump` "no longer active", `field pump two` "not found — no status recorded". The day-1000 kiln's own retirement fact carried no subject and could not be attributed → re-teach in act three |
| G-17 | **W (root cause of the twin family)** | **The entity resolver merged distinct twins as aliases.** D-0075 name-variant resolution (`EntityMemory.resolveCanonical`: exact/alias hit → fuller name canonical; else a judge verdict over lexically similar candidates → fuller name canonical, the other name becomes an alias and its row is superseded) treated a separately-taught shorter twin as a *variant* of the longer one — the judge template's own model case of SAME is "a short name vs its full name ('Pepper' ⇄ 'Pepper Potts')", which is exactly `coral census` ⇄ `coral census two`. From then on every exact lookup of the shorter name resolves to the twin **by design** and the twin's value is stated with full confidence (wrong values, not honest misses). The merge happens when the TWIN is first taught (the judge compares the new mention "Coral Census Two" with the existing "coral census" and says SAME); the only trace is the `resolvedFrom` field on 29 `entity_remembered` audit events — never announced, so nothing surfaced it in 1000 days | Day-1000 world: 29 of 179 active entities carry aliases, and the aliases are the shorter twins — `Coral Census Two {coral census}` (the `coral census` row superseded), `Microscope Two {microscope}`, `Aquarium Rig Two {aquarium rig}`, `Rooftop Garden Two {roof array, roof array two}`, `kiln north {kiln, the kiln}`, `Lakeside Cabin North {lakeside cabin}`, `glacier telemetry north {glacier telemetry}`, `sensor importer north {sensor importer two}` (two different twins merged), `seed vault {seed bank}` (chapter two absorbed a base entity), `catering service two`, `cold cellar two`, `the test range`, `3D printer north`, …; 161 entity-resolution judge calls over the life. Explains `coral census` 5/5 wrong (always the twin), the 22 strict two-hop twin misses, both `kiln` retirement misses, and the hedges of the "two similarly named entities" shape. Identity-first seeding (D-0080 S1) cannot help when the store itself says the two names are one entity | **FIXED+VERIFIED 2026-09-11** (`REFINEMENT_2026-09-11.md` R3) | (1) `qualifierTwin` rule: twins are never judge candidates, an affirmed twin is declined on the record, a leftover twin alias is neither a write hit nor a lookup match; judge template corrected; (2) every judge merge audited (`entity_alias_merged`) + announced; (3) `splitTwinAliases` / `POST /memory/reconcile-twins` applied to the day-1000 world: **10 splits** (audited + announced, nothing deleted), **9 unsplit** (facts carry no subject — `coral census`, `microscope`, `aquarium rig`, `lakeside cabin`, `glacier telemetry`, `sensor importer two`, `catering service`, `the morning swim`, `lena moreau`) → chapter three's re-teach set; (4) 12 tests; suite 507/507. Discovered alongside: the agent writes facts WITHOUT their subject ("Status colour is teal") — see G-18 |
| G-18 | W / M | **Facts are stored without their subject.** The agent's `rememberFact` statements read "Status colour is teal", "Service day is Monday" — the entity is implied by the row, never by the text. Harmless while the entity row is right; fatal once two things share a row (G-17): nothing in the store can say which sibling a fact belonged to, so 9 folded twins could not be split from evidence | Day-1000 world: the merged twins' facts (`Coral Census Two`: "Service day is Monday", "Status colour is teal", …); the 9 `unsplit` twins | **FIXED 2026-09-11** (R6) — `withSubject()`: a statement that does not name its entity is stored prefixed with it ("coral census two: Status colour is teal"), single and batch tools; description asks for the subject; Sonnet 5 now writes "gantry crane two's status colour is teal" itself. Existing subject-less facts stay as they are (their twins are re-taught in act three) |

| G-19 | W / M (found in R10) | **A status statement written as a correction superseded an unrelated attribute.** "We've wrapped up X — consider it closed" went through `memory.correct` (no factId, no `replaces`); the target picker chose the entity's only fact, "X's status colour is slate", on the one shared content word "status", and retired the colour with the closure | Sonnet-5 mini-life run 2 (`refinement/R10_minilife_sonnet5_run2.md`): `sluice gate north` audit `fact_corrected … superseded: 1`, the battery then answered "status colour: not found — decommissioned"; day-1000 world: 1 of the 6 act-two retirements did the same (`fusion sim north`, run seq 22668–22690, one fact superseded), the other five used `rememberFact` | **FIXED 2026-09-11** (R10) — `slotCompatible`: a guessed correction target must be about the SAME attribute slot as the new statement (both parsed by `parseSlot`; slots must be identical); a different-slot statement becomes a NEW fact, an explicit factId on another slot is refused unless `replaces` confirms; same-slot corrections unchanged. Test from the captured statements; **mini-life run 3 on the final build: the retired twin keeps its status colour, battery 5/5 strict** | `fusion sim north` added to the chapter-three re-teach set; retirements (T6) measured in act three |
| G-20 | O / D (found in the D-0082 calibration) | **The memory judge's 15 s timeout is a constructor constant, not a catalogued setting.** On a model with mandatory reasoning (Qwen 3.8 Max via OpenRouter, judge calls 2–14 s) a latency spike aborts the call silently and the best-effort contract falls back to the deterministic path — for topic extraction that means `judged` stays 0 and a genuine correction never promotes | Qwen mini-life (`refinement/R11_minilife_qwen38max_run1.md`): both "helium recovery" extraction calls aborted at 15,003 / 15,001 ms right after a kernel restart; the topic did not promote (the junk control and every memory shape passed). Real act days 1001–1003: 0 aborted in 201 OpenRouter calls (judge avg 2.8–3.6 s, max 6.6 s); shakeout: 0 aborted in 325 (max 11.5 s) | **FIXED 2026-09-13** — `memory.judge.timeoutMs` is a catalogued setting (D-0053, 2 s–120 s, default 15 s) read LIVE through a resolver, so a slower judge model is a config change instead of a silent fallback; a resolver that throws or returns nonsense falls back to the 15 s default rather than leaving a write uncapped. 2 tests (a raised cap lets a slow judgment through while a low one abandons it; a broken resolver still caps). Landed during the credit halt, before the act's measured days | T7 (G-09 promotions) is the exposed target |
| G-21 | W (legacy) / R | **Two entity rows differing only by the leading article.** `boat shed north` (Bergen, the truth) and `the boat shed north` (Hobart, stale) are separate rows; `recallGraph` returns both as identity seeds and the agent can answer from the stale one. R3 made `findEntity` resolve `X` ⇄ `the X` and prevents NEW splits, but nothing merges rows that already exist (`reconcileHomes` works within one entity; `splitTwinAliases` splits rather than merges) | Five pairs in the day-1000 world: `cloud recycler`, `cloud recycler north`, `boat shed north`, `coral census north`, `tidal model north`; **three are in the chapter-three re-teach set**, asked 15 / 8 / 8 times in act two (≈31 questions per 500 days). Created in acts one and two (audit `entity_remembered` seq 4115 / 5876 / 10913, 2026-08-31 and 2026-09-10), not by this build. Cost a strict miss in the act-three shakeout (`longitude_xl/SHAKEOUT_ACT3_2026-09-12.md`) and **reproduced in the real act on day 1010** — the act's first battery scored 25/25 lenient / 24/25 strict and its single miss is this shape, the agent again naming the cause itself: *"hobart (entity 'the boat shed north'; note a separate 'boat shed north' lists bergen)"*. The stale row persists after the re-teach, so the drag continues for the rest of the act | **FIXED 2026-09-13** — `EntityMemory.reconcileArticleVariants()` folds an article variant into the plain name (the form the world teaches and `withSubject` writes): active facts and relations migrate forward conflict-safely, the folded spelling is kept as an alias so the old name still resolves, the row is superseded with `superseded_by`, and the change is audited (`entity_article_variant_merged`) and announced. It runs in the quiet-hours sleep cycle BEFORE the one-home pass, so the two values meet on one entity and `reconcileHomes` retires the older in the ordinary way; route `POST /memory/reconcile-articles` (dry-run by default). Deliberately narrow — only a leading article may differ, so a qualifier twin can never be folded. 1 test (fold + migrate + alias + twins untouched + dry-run changes nothing). **Applied to the day-1000 world 2026-09-13** as attempt-5 world prep: exactly the five predicted pairs folded (`the cloud recycler north`, `the cloud recycler`, `the tidal model north`, `the coral census north`, `the boat shed north`), audited ×5 and announced | After the act: a merge route for article-variant rows — the mirror of `splitTwinAliases`, audited + announced + non-deleting — plus a sweep of the world; ships with the G-20 timeout knob |
| G-22 | I (scorer) | **The strict rubric scores a correct answer as a twin substitution when it annotates its twin without repeating the asked name.** `names_in()` blanks the longest name first, so in "sencha (your weekend evening drink is chamomile)" the only occurrence of `evening drink` sits inside `weekend evening drink`: the asked entity looks absent, a twin looks present | Act-three shakeout day-1010 battery: 25/25 lenient, 23/25 strict, one of the two misses this shape (the stated value WAS the truth). Systematically under-scores the 28 weekend-twin preference topics | **FIXED 2026-09-13** — a twin SUBSTITUTION is one that answers with the twin's value: the verdict now requires the stated value to differ from the asked entity's truth, so a correct answer that annotates its twin stands as a hit; for retirements (no value to compare) the twin must carry the LEADING clause. The first form of the fix was too permissive and the harness's own dry-run gate caught it: an answer whose SUBJECT is the twin ("the coral census two's status colour is teal") is a substitution even when the value coincides. Final rule: a twin that LEADS the answer, or a twin named while the stated value is not the asked entity's truth. 10 rubric shapes tested. Re-running the re-score over acts one and two reproduces the published rows BYTE-IDENTICALLY (0 of 2,166 verdicts change), so no published figure moves — the shape only appears in act three, where the kernel's newer look-alike annotations produce it. Fixed before the act's measured days rather than post-hoc | Rule: when the asked name occurs only inside a longer twin, look for the stated value in the clause that does not name the twin; publish as-run and re-scored figures side by side |
| G-23 | **System (gateway)** — found in the field 2026-09-12 | **`thinking: "off"` did not disable reasoning on an OpenAI-compatible provider.** The adapter translated "off" by OMITTING `reasoning_effort`, which means "no preference", not "off" — and every Qwen model on OpenRouter reasons by default. The fast role and the D-0075 memory judge therefore thought on every call despite being pinned off | First minutes of the D-0083 run: `qwen/qwen3.8-flash` averaged **10.6 s and 566 output tokens** per call through the kernel, against **1.4 s and 33 tokens** on a direct probe that sent `reasoning: {enabled:false}`. The switch made to cut wall-clock was delivering none of it | **FIXED 2026-09-12** — provider config gains `reasoningDialect: "openrouter"`; when set, `thinking: "off"` sends OpenRouter's documented `reasoning: {enabled:false}`. Never sent by a provider that does not declare the dialect, because plain OpenAI-compatible servers reject unknown body fields. 3 tests (off → switch + no effort; thinking target → effort + no switch; undeclared provider → byte-identical body); full suite **529/529, 0 skipped**. After the fix the same role answers in 2–6 s with 38–116 output tokens | Act three relaunched from day 1001 on the fixed build; the two aborted attempts are preserved and excluded |
| G-24 | M (agent behaviour) — found in act three, day 1009 | **A re-teach recap read the look-alikes and stored nothing.** The recap "the aquarium rig's status colour is … ; the aquarium rig's service day is …" ran `memory.recall aquarium rig` → "no memory", then recalled `Aquarium Rig Two` and `Aquarium Rig North`, and ended the run WITHOUT a write. The next recap (assigned number) created the entity and stored its fact normally. Net effect: a re-taught fact never landed, and the day-1010 battery answered it wrong | Audit seq 29214–29222 (three `memory.recall`, no `fact_remembered`) against seq 29240–29247 (`entity_remembered` + `fact_remembered` + read-back receipt). Day-1010 battery: `aquarium rig` service day answered "monday", announced truth "thursday" — the only re-teach miss of 5 asked. The same recap DID land in attempt 1 on the other model, so this is variance in agent behaviour, not a deterministic defect | **OBSERVED 2026-09-12**, agent-behaviour variance; **instrumented for attempt 4 (harness, not kernel)**: `assert_recaps_landed` checks every fact re-taught that day on the kernel (the entity row of exactly that name, or the preference key) and re-issues a missing one ONCE with an explicit write instruction, logging `[recap-miss]` and recording `recap_noops` / `recap_retried_ok` per day — a silent no-op is now counted the day it happens and repaired once, never hidden | The rate at each preserve comes from the metrics; the agent prompt for a recap requiring a write per clause remains a kernel-side option if the retry rate stays high. Track the rate at each preserve: 1 of 47 recapped facts by day 1010; **2 of 93 by day 1020** — the second is `tea order` (day 1003): its plain `tea_order` key had been DELETED on simulated day ~328 by the pre-S2 preference dedupe (the twin `weekend_tea_order` absorbed it, Defect D), the recap "my tea order is darjeeling" ran `memory.recallPreferences` (3 matches, among them `weekend_tea_order = darjeeling`) and ended without a write, and the day-1020 battery answered "darjeeling (weekend tea order)" — a strict twin miss, audit seq 28907–28911 **ACT-THREE VERDICT (2026-09-14): CLOSED — the instrument worked and the layer it guards scored 97.5 %.** Over 500 days: 8 recap no-ops raised, 3 recovered by the single retry, and the remainder proved to be the instrument's own blind spots (G-29/G-30 name shapes), every one confirmed stored in the audit. Re-taught facts finished **269/276 (97.5 %) strict, 81/82 (98.8 %) on the first battery after each recap** — T2's target was ≥ 90 %. |
| G-25 | **System (gateway) + instrument** — found in the field 2026-09-12 (act three, days 1013–1015) | **A retryable provider error on a single-target role fails the call outright, and the failure is invisible to the caller.** The router marks HTTP 429 `retryable` but never retries the same target, and with no second target on `fast_conversation` every 429 became a failed call. Downstream, each layer did what its contract says and the sum was silent: the memory judge fell back to the deterministic path (best-effort by design), the converse stream ended with an error event the harness does not read (it counts tokens only), so 9 chats were "answered" with an empty string | OpenRouter rate-limited `qwen/qwen3.8-flash` 22:37:01–22:51:02 UTC: 107 of 219 calls failed (`model_calls` ids 54610–54816); 96 judge fallbacks (16 / 55 / 25 on days 1013 / 1014 / 1015), 9 empty chats (4 / 5 on days 1014 / 1015), 2 deterministic topic extractions (day 1014); wall-clock 252 / 454 / 349 s against the 158 s median; cleared by itself — day 1016 had 0 failures. The facts written inside the window are listed in the runbook ops log (`boat shed`, `boat shed north`, `morning swim`, `morning swim north`, `gym day`, `study plant`, `jiro nwosu`, the `3d printer` / `3d printer north` relation families) | **FIXED 2026-09-12 (build a7dd92b, attempt 4)** — `GatewayRouter` retries a retryable pre-stream failure on the SAME target with jittered exponential backoff (750 ms base, ×2, cap 8 s) before the fallback chain moves on; every attempt stays its own `model_calls` row; knobs `gateway.retry.maxRetries` (default 2) and `gateway.retry.baseDelayMs` are catalogued settings (D-0053) read live per request. Harness: an SSE `error` event or a token-less reply is logged `[empty-reply]` and counted per day (`empty_replies`). Tests: retry-then-succeed on one target (3 attempts, 2 failed rows + 1 ok, no fallback), retries exhausted → fallback, non-retryable → no retry; 539/539 | Attempt 3 (days 1001–1035) stays the record of the unguarded behaviour; a second upstream target for the fast role remains a configuration choice for a future act |
| G-26 | **System (memory) — found in act three, batteries 1010 and 1020** | **A taught nickname is stored as a fact, never as an alias, so the exact-entity rule now declares it "not found".** "X usually just goes by Y — same person" has no write path to `memory_entities.aliases` — the column is filled only when the D-0075 judge merges a mention into an existing entity at write time, and no tool takes an alias — so the agent writes a fact ('usually goes by "Theo" — same person'). Identity matching (`identityMatch`: names and aliases) never reads facts, and the R10 exact-entity rule then opens the answer with "not found for an entity named exactly 'ravi' — the closest match, ravi lindholm, meets on tuesday": right value, declared miss under both rubrics. The world never uses the nickname in a later teach, so the judge never gets the chance to alias it | Act two (old build, before the not-found rule): 30/40 alias questions hit leniently. Act three: 0/2 (`ravi`, days 1010 and 1020, class honest). All four chapter-two people carry `aliases = NULL` (two still hold a "goes by" fact, two lost it), and the twelve chapter-three handles (first at day 1054) reach the same code path, so T5 (100 %) is structurally at risk on this build regardless of the model | **FIXED 2026-09-12 (build a7dd92b, attempt 4)** — the nickname has a home: `EntityMemory.addAlias` (audited `entity_alias_added`, read back; refused when the alias already names another active entity — an ambiguous handle stays a question, G-12 — or is a qualifier twin of the entity's own name, G-17) behind the new `memory.alias` tool and an `aliases` argument on `memory.rememberEntity`; a naming statement offered to `memory.rememberFact`/`rememberFacts` ('usually goes by ravi — same person') is refused with the exact alias call handed back (`aliasInDisguise`); the agent prompt names the rule; a declared alias seeds identity recall as a whole word at any length (`aliasMatch` — the short-name gate stays for incidental names). Tests: alias read paths (`recall`, `recallGraph` seeds via a 4-letter handle), clash and twin refusals, rollback, tool round trip through `memory.lookup`; 539/539 | Attempt 3's 1/3 on `ravi` is the record of the old path; attempt 4 measures T5 on the fixed build **ACT-THREE VERDICT (2026-09-14): FIXED, TARGET MISSED — the path now exists and carries traffic, but T5 wanted 100 % and got 86.0 %.** `entity_alias_added` fired 4 times in the act (first `jiro nwosu` → `jiro`); alias questions scored 43/50 strict overall, and by century 6/10 → 10/10 → 9/10 → 8/10 → 5/5, i.e. **37/40 (92.5 %) once the chapter-three handles were flowing** against 6/10 in the first century before they started. In attempt 3 this layer was structurally unreachable (every person `aliases = NULL`), so the fix is real; the target is still missed and is reported as missed. |
| G-27 | **System (autonomy)** — found in act three, day 1025 | **A D-0052 override of a user-set value is announced on the timeline only, never through the D-0077 chat relay.** `SleepCycle.run()` puts the override into the consolidation report's `adjustments` (with the tally and the re-pin advice) and records the report as a `sleep-cycle` episode; the scheduler never hands it to the `Announcer`, so no `announcements` row exists and the next conversation does not relay it. The user learns that their pin was changed only by opening `/pulse` or asking | Day 1025, 23:17:23 UTC: `reasoning_autotune` → threshold 2→1, source `jarvis`, `changedUserSetting: true`, reason "24 contradictions since your setting … cleared the bar of 24"; episode detail "changed your manual setting 2→1 — the trail outweighed the pin (24 ≥ 24); re-set it and I'll hold it twice as long"; `announcement_raised` rows since the pin: none from the sleep cycle (only night-lab and twin-split notices). T8's "announced with the tally" is met on the timeline, not in chat | **FIXED 2026-09-12 (build a7dd92b, attempt 4)** — `SleepCycle` takes an `announcer`; an applied D-0052 override raises one advisory announcement (dedupe key `sleep-cycle:override:<pin time>`, source `sleep-cycle`) carrying the tally, the bar and the re-pin advice, so the D-0077 relay says it in the next conversation; the timeline episode also now names memory merges and declined merges. Test: an override raises exactly one announcement, a respected pin raises none; 539/539 | The day-1025 override of attempt 3 is the record of the silent path |
| G-28 | **System (memory) — found in act three, day 1030 battery** | **Judge-driven consolidation merged two distinct-attribute facts into an unrelated fact, silently.** `EntityMemory.consolidate()` applies the fast-model `mergeFacts` groups with no slot check ("trust the model's call"): on the night of day 1027 it folded `theo eriksen meets on Tuesday` and `theo eriksen is based in Lisbon` into the nickname fact `theo eriksen usually goes by "Theo"`, so the day-1030 battery answered "no location/based-in fact is recorded". The merge is not audited per fact (only `memory_consolidated {duplicatesMerged: 2}`) and the report says "merged 2 duplicate fact(s) across 200 entities" without names, so nothing announced what was lost. The deterministic fallback would never have merged them (it needs containment or Jaccard ≥ 0.7) | `memory_facts` for `theo eriksen`: two rows `superseded`, both `superseded_by` the nickname fact's id; audit seq 29973. Rate: 3 merges in 31 act-three nights on the Qwen 3.8 Flash judge against 10 in the 1,076 nights before (Haiku 4.5 / Sonnet 5), so the judge model matters and the guard is missing either way. History is intact: both statements are recoverable by restoring their status | **FIXED 2026-09-12 (build a7dd92b, attempt 4)** — `judgeMergeAllowed`: a judge merge may fold a fact only into a fact about the same thing (both parse to a slot → the slots must agree; otherwise at least one shared content word beyond the entity's name — the paraphrase case the judge exists for); declined merges are returned (`refused`), counted in the `memory_consolidated` audit row (`mergesRefused`) and listed in the report and the timeline episode; every merge, judge or heuristic, is audited per fact (`fact_merged_by_consolidation`, ids only) and announced by name through `onMemoryChange` ('fact-merge'). The theo eriksen facts are intact in the restored day-1000 world (the merge happened in attempt 3). Tests: three same-entity facts on three slots survive a judge that says "merge all" (0 merged, 2 refused); a same-slot paraphrase still merges, audited and announced; the pre-existing paraphrase and heuristic tests unchanged; 539/539 | Merge rate per night stays on the Routine's watch (`memory_consolidated` payloads) so attempt 4 shows the guard's effect against attempt 3's 3-in-31 |
| G-29 | **W (legacy) / I (instrument)** — found live 2026-09-13, act three attempt 5 day 1003 | **A possessive phrase became an entity, then absorbed the real person.** The day-1000 world carries four rows whose canonical name is an attribute phrase — `Lena Moreau's meeting`, `Tessa Novak's meeting`, `quinn lindholm's meets`, `Diego Mbeki's Meets`. For one of them the D-0075 fuller-name rule then made the phrase CANONICAL and superseded the clean `Lena Moreau` row, keeping `lena moreau` only as an alias, so the person is now filed under one of her own attributes. Recall still works (the alias resolves), but every later fact accretes onto the attribute-shaped row | All four rows are present in `jarvis_xl.day1000-pre-act3.sql.gz` (the phrase appears 9× in the dump) with world timestamps in 2024-09 — created by an EARLIER build, not by this one; the day-1003 write behaved correctly given the alias (`fact_remembered` on the canonical row, read back). Surfaced by the G-24 recap instrument, whose check required the canonical name to EQUAL the topic and so re-issued the recap every day for a fact that was in fact stored | **OBSERVED 2026-09-13** (world damage, not a live defect — no mid-act system change). The INSTRUMENT was repaired mid-act and disclosed, TWICE on the same day as two different false alarms surfaced: a topic counts as on file when the store resolves it to that entity by name OR by a recorded alias (a qualifier twin still cannot satisfy it, G-17), OR when a PREFERENCE whose key covers the topic holds the value. The second case came from `archive digitisation` on day 1004, and the world was entirely correct there: its assigned number (19) lives in the preference store, and the kernel's one-home guard (R-MEM-08) rightly REFUSED a duplicate entity fact, telling the agent to use `memory.correct` — nothing needed correcting, since the stored value already matched. Checking entity facts alone would have re-issued that teach on every re-teach day for the life of the act. Provably inert for the committed days — days 1001–1002 had no re-teach due and 0 `recap_noops` — so the act stays on one build and one configuration | After the act: extend the G-21 reconciliation family with a possessive/attribute-name pass — an entity named `<known entity>'s <attribute word>` is folded back into the base entity with its facts migrated, audited and announced; and make the entity resolver refuse to promote an attribute-shaped name to canonical over a clean personal name |
| G-30 | **W (legacy) / I (instrument)** — found live 2026-09-14, act three attempt 5 day 1014 | **An article-prefixed CANONICAL name with no plain row for the reconciliation to fold into.** Seven active rows in the day-1000 world are named `the X` / `The X Two`: `the morning swim north`, `The Sensor Importer Two`, `the test range`, `the kiln`, `the lena moreau`, `the monthly backup north`, `The Filament Shop Two`. For NONE of them does a plain-named row exist, so D-0085's `reconcileArticleVariants()` — which folds `the X` INTO an existing `X` — structurally cannot reach them, and the world-prep pass at attempt-5 launch correctly folded only the five pairs where both spellings existed. The article row then accumulates the world's facts under a name the world never says, and attributes end up with two homes across the variants (`morning swim north's core material …` alongside `the morning swim north's core material …`) | The kernel is CORRECT here: it resolves `morning swim north` to the article row and stores facts on it with read-back receipts — audit seq 29774–29783 shows `memory.rememberFact` ok + `fact_remembered` on `the morning swim north` for all three day-1014 re-teaches. Only the G-24 recap checker, comparing raw strings, called it "not on file". SQL: 7 rows match `^(the\|a\|an) ` on `memory_entities` with `superseded_by is null`, 0 of them have a plain twin row | **OBSERVED 2026-09-14** (world damage, not a live defect — no mid-act system change). The INSTRUMENT was repaired mid-act and disclosed, the THIRD such repair in this family: `kernel_has_value` now strips a leading article on BOTH sides before comparing, which leaves the G-17 twin exclusion exactly as it was (`morning swim` still never matches `morning swim north` — the qualifier, not the article, is the identity difference). Verified live against the running kernel: correct value → True, wrong value → False, plain twin → False. **Not inert for the committed days, and this is stated rather than smoothed over:** days 1008, 1012 and 1014 each carry `recap_noops` that were instrument artifacts, and each triggered one extra re-issued teach into the measured world (day 1012's "recovered" retry is the same artifact). The facts were already stored, so no value was lost; the perturbation is 4 extra agent runs across 3 days, named here so the act's record can account for them | After the act, in the same pass as G-29: extend the reconciliation family to normalise an article-ONLY canonical name — rename `the X` to `X` when no `X` row exists, keeping the old spelling as an alias, audited and announced — so the fold no longer requires a plain row to already be there |
| G-31 | **W (legacy)** — found live 2026-09-14, act three attempt 5 around day 1060 | **The G-17 twin split left a stale alias behind, so six plain names are simultaneously an entity AND an alias of their qualifier twin.** `aquarium rig`, `catering service`, `coral census`, `lakeside cabin`, `microscope` and `glacier telemetry` each exist as their own live row while still appearing in the `aliases` array of `Aquarium Rig Two`, `Catering Service Two`, `Coral Census Two`, `Lakeside Cabin North`, `Microscope Two` and `glacier telemetry north`. The R1–R10 refinement split the folded twins from evidence and created the plain rows, but did not retract the plain name from the twin's alias list. A seventh is worse-shaped: `sensor importer north` carries the alias `sensor importer two`, a twin name pointing at a different twin | **Latent, not active — verified live on the running kernel:** all six plain names resolve to their OWN row (`coral census` → `coral census`, `aquarium rig` → `aquarium rig`, and so on), because exact name beats alias under the R10 identity-first rule, so no answer is wrong today. Only one alias in the whole world was written by THIS build — `jiro nwosu` → `jiro`, audit `entity_alias_added` ×1, the chapter-three nickname (G-26 working). Every other alias predates the act | **OBSERVED 2026-09-14** — recorded, not patched mid-act. Severity is "one resolver-precedence change away from wrong answers": the damage is invisible while exact-match wins and would surface the moment alias matching were widened, which is exactly the kind of latent trap the twin work exists to remove | After the act, with G-29 and G-30: a reconciliation pass that retracts an alias once a row of that exact name exists (audited and announced), plus a write-time guard so `addAlias` refuses a name already held by a live entity — `aliasMatch` already refuses a qualifier twin at write time, so this is the same rule applied to the legacy rows |

## Efficiency gaps (cost, tokens, latency, wall-clock)

Measured on the whole life through day 1000 ($172.54 in the world's ledger; first act $70.09, second act $102.45 — attempt 1 of the second act, rolled back, is not in the table). Sources: `model_calls` (tokens per call by role), `metrics.jsonl` (spend and latency per day), `audit_log` (steps per agent run, tool result sizes), `lab_experiments` (tokens per experiment). Cost split below at list prices (Sonnet 5 $3/$15, Haiku 4.5 $1/$5 per M input/output tokens), which reproduces the ledger total within a dollar.

| ID | Gap | Evidence | Candidate refinement |
|---|---|---|---|
| E-01 | **The planning role's per-step context is the cost of the whole exercise.** Every agent step re-sends ~12.6k input tokens (tool catalogue + injected context + conversation) with no prompt caching | `planning` (Sonnet 5): 3,304 successful calls, avg **12,650** input / 200 output tokens → 41.8 M input + 0.66 M output tokens ≈ **$135 of $172.54 (78 %)**. `fast_conversation` (Haiku 4.5): 40,708 calls at 348/62 tokens → 14.2 M + 2.5 M ≈ $27 (16 %). `deep_reasoning` (Sonnet 5): 1,454 calls at 1,171/243 → 1.7 M + 0.35 M ≈ $10 (6 %). Embeddings: 3,240 calls, local, $0 | **FIXED+MEASURED 2026-09-11** (`REFINEMENT_2026-09-11.md` R8) — the adapter marks the system block and the last tool definition `cache_control: ephemeral`; cache reads/writes recorded in `model_calls` (migration 0028); `toolScope` trims the catalogue per run. Measured on a 5-question battery: 4–5 planning calls × ~12.6k uncached → 2 calls × ~2k uncached + a 13.2k prefix read from cache |
| E-02 | Quiz batteries cost 6–8× an ordinary day and dominate spend on quiz days | plain day $0.10 (act 1) / $0.13 (act 2); quiz day $0.47 / $0.84; quiz+lab day $0.67 / $1.09. Battery runs reach 8–10 agent steps per 5-question batch (avg 3.2 steps per run overall; every ≥8-step run in the last 400 was a quiz batch) | **FIXED+MEASURED 2026-09-11** (R8) — `memory.lookup(queries[])` answers several questions in one call (named entities with facts/relations, look-alikes flagged, ranked preferences, first-person facts); Sonnet 5 chose it unprompted for a 5-question battery: 1 tool step, 5/5 correct |
| E-03 | Night Lab spends about a quarter of the budget for one keep | 50 lab nights over the life (≈ $39.5 = 25 % of spend measured at day 931); at day 1000: 67 experiments, **2.54 M tokens**, 65 discards / 1 crash / 1 keep (the keep cost 73 k tokens). Corrected reading (R7): discards are "no improvement on first trial" against a baseline near its ceiling (~93), not cap-truncated trials. **ACCEPTED with a recommendation for the next check-in:** fewer lab nights (e.g. weekly) or a smaller bench while the baseline sits at its ceiling; the cap overshoot itself is fixed (G-08) | See G-08: size the cap to the bench (or the bench to the cap); skip nights whose baseline already crashed; keep-rate per completed night as the metric |
| E-04 | `memory.recallPreferences` can return the whole store into the agent's context | avg 7.2 matches per call, **max 94**; "gym day" matched 26 preferences (every key containing "day") | **FIXED 2026-09-11** (R8) — `rankPreferences`: whole-token overlap, exact keys first, cap 12 with an honest "N more match loosely"; unrelated queries match nothing. Test |
| E-05 | Wall-clock per simulated day doubled in the second act | median day 43 s (act 1, 7.7 h total) → 94 s (act 2, 13.7 h total): chapter two, cross-link teaching, larger batteries, hourly heartbeats | CLOSED as a finding (R8): experiment load, not the system — per-step latency flat across 1000 days |
| E-06 | Deep-reasoning calls are 4% of calls but the second-largest per-call context | 1,355 deep calls, 1,169 input tokens each; almost all are the harness's routine forced-deep controls (2 per day), not learned-topic escalations (`deepOnAuto` fired 5 times in 430 days) | CLOSED as a finding (R8): the learned reflex is precise; the count is the instrument's control arm |
| E-07 | Positive finding worth keeping: per-call input does **not** grow with memory | fast-role input tokens per call by life quartile: 493 → 351 → 289 → 285; latency flat | CLOSED as a finding (R8): the context injector's caps work; the E-01 trim keeps the prefix cacheable |

## How to refine from here

1. Strict re-score of every battery from the raw answers (G-13), then re-derive the layer numbers.
2. Rank by attributable misses over days 501–1000: G-01 and G-03 dominate the old-world 8.7 % (63 misses); the twin family G-02/G-04/G-16 accounts for 5 single-hop wrong values, 7 two-hop misses and 2 retirement misses; G-05 is all 7 chapter-two misses; G-07 3 two-hop misses. The cheapest wins are G-05 (two small levers, three captured objectives as tests) and G-16/G-02 (one recall-fallback rule).
3. Each fix gets the D-0080 treatment: spec, unit tests from the exact captured tool arguments, a Sonnet-5 mini-life. E-01 (prompt caching + tool-catalogue trim) is worth doing before the next act — it is 78 % of the bill.
4. Then a **third act on the preserved day-1000 snapshot with chapter three** (late deep-topic corrections G-09, a late user re-pin G-10, unique nicknames G-12; re-arm the relaunch Routine and keepalive, G-14) so the field verification stays on one kernel build. `jarvis_xl` and `/tmp/longitude_xl` are preserved for it — never reset the world.

## Retest on one build (R10, 2026-09-11) — status of every row

Build `76f2e6b`; full suite 526/526, 0 skipped; day-1000 replay before/after
(`longitude_xl/day1000/replay_{before,after}_r10.json`); Sonnet-5 mini-life
runs 1–3 (`refinement/R10_minilife_sonnet5_run{1,2,3}.md`, final run 25/25).
Details in `REFINEMENT_2026-09-11.md` R10.

| Status | Rows |
|---|---|
| FIXED+VERIFIED (live shape passes in the mini-life on the final build) | G-02, G-03, G-04 (lever), G-05, G-07, G-09 shape (promotion + junk control), G-10 shape (D-0052 at the raised bar), G-15, G-16, G-17, G-18, G-19, E-01, E-02, E-04 |
| FIXED (unit-tested; no live shape in a mini-life) | G-08, G-11, G-12 (instrument) |
| PREVENTED (world-side closure in act three, T2) | G-01 |
| CLOSED | G-06 (into G-05), G-13 (adopted live), E-05, E-06, E-07 (findings) |
| ACCEPTED with a check-in recommendation | E-03 |
| DOCUMENTED, re-armed at launch (T12) | G-14 |
| DESIGNED-INTO-ACT-3 (T7, T8) | G-09, G-10 |

Nothing on the ledger is open without a named closure path; the third act
(`longitude_xl/RUNBOOK_ACT3.md`) is the field retest on this build.
