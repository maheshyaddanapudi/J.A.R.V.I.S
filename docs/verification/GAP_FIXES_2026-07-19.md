# Gap-fix verification — 2026-07-19 (D-0062)

Closes all 5 gaps recorded in `SCALE_EVOLUTION_2026-07-18.md`. Clean-slate DB
(`jarvis_scale2`, dropped after), real Anthropic brain (key vault-only, scrubbed
after: 0 hits repo + scratch), rebuilt kernel. Everything below is live-API +
DB ground truth.

## What changed
| Gap | Fix | Where |
|---|---|---|
| 1 No agent forget/correct | `memory.correct` (LOW_REVERSIBLE) + `memory.forget` (CONSEQUENTIAL, entity or single fact) | `memory/entityTools.ts`, `memory/entities.ts` |
| 2 `superseded_by` null | Entity + fact supersession link forward to the replacement, transactionally | `memory/entities.ts` |
| 3 Near-duplicate knobs | `register()` refuses normalized-key collisions + label overlap ≥0.6 (system+dynamic), points at the existing key | `settings/registry.ts` |
| 4 Orphaned A2UI panels | `settings.onRemove → a2ui.pruneSetting` (prune component; remove empty panel) | `a2ui/registry.ts`, `core/index.ts` |
| 5 No key rediscovery | READ_ONLY `settings.list` tool | `settings/tools.ts` |
| (design) heuristics → read-then-write | recall surfaces `factId`s; `correct`/`forget` take exact ids (stale id refused); text-match only as fallback | user's direction, this session |

## Live proof (real brain, per fix)
- **1+RW:** objective "training moved to Mon/Thu; he no longer does kettlebell —
  drop that one, don't replace it" → agent chose `memory.recall` →
  `memory.correct(factId)` ×2 (caught BOTH differently-worded schedule facts —
  text matching had missed one) → `memory.forget(factId)` for exactly the
  kettlebell fact. After: corrected facts present, stale gone, unrelated facts
  untouched. The LLM listed, then decided insert/update/delete — no guessing.
- **2:** DB: every `superseded` entity/fact row carries `superseded_by` → the
  replacement id (Reactor v1→v2; corrected facts). History walkable.
- **3:** `zylo_threshold` registered → "Zylo Threshold Level" REFUSED
  ("similar setting already exists — 'zylo_threshold'") → "Zylo Cadence"
  allowed. Bonus: refusal also fires against SYSTEM keys (a "Quiet Hours Start"
  register was refused against catalog `proactive.quietHours.start`).
- **4:** composed a panel bound to `briefing_tone`, deleted the setting →
  panel pruned automatically; a panel that became empty was removed; 0 truly
  dangling refs across all panels (checked against system+dynamic keys).
- **5:** objective "find the weekend-mode key and delete it" → agent ran
  `settings.list` → `settings.reset("weekend_mode")` → gone.

## Aggregate re-run (organic use, clean slate)
30 chat sessions / 110 turns (30/30 continuity, 30 deep auto-escalations) +
45 agent runs + 5 skills, 0 failures, 0 HTTP-400. Organic tool mix now includes
`memory.correct`×6 · `memory.forget`×5 · `settings.list`×10 (none existed
before). 6 settings registered — **zero near-duplicates** (vs 2 overlapping
quiet-hours representations in the previous run). Recall after modification
returns the corrected values ("Happy is the Chief of Security", "Pepper is now
CEO"). Audit chain intact (1,038 entries).

## Tests
**302/302 kernel tests** (up from 292): +14-total entity suite (factId
targeting, stale-id refusal, single-fact forget, word-overlap fallback,
forward links), near-dup matrix, `settings.list`, `pruneSetting`.

## Honest residual notes
- Correction wording that shares NO content words with any stored fact still
  falls through to "recorded as new" (reported in the tool summary, which asks
  the model to recall + use a factId) — by design: never supersede on a bad match.
  The read-then-write path makes this rare (the model sees ids up front).
- Aggregate runs sometimes still store near-restatements via `rememberFact`
  before deciding to correct; entity re-remember + recall ordering keeps the
  surfaced answer correct. Further dedup at fact-write time would trade write
  latency for it — not taken now.
